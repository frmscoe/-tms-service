version: 1.0
provider:
  name: openfaas
  gateway: OPENFAAS_URL
functions:
  off-transaction-monitoring-service-rel-1-0-0:
    namespace: openfaas-fn
    lang: dockerfile
    handler: .
    image: DOCKER_REGISTRY/off-transaction-monitoring-service-rel-1-0-0:latest
    labels:
      com.openfaas.health.http.initialDelay: "45s"
      com.openfaas.profile: nodeselector
# The following labels are not available in openfaas community edition.
#       com.openfaas.scale.type: rps
#       com.openfaas.scale.min: 1
#       com.openfaas.scale.max: 10
#       com.openfaas.scale.zero: false
#       com.openfaas.scale.zero-duration: 15m
#       com.openfaas.scale.target: 50
#       com.openfaas.scale.target-proportion: 0.90
    secrets:
      - frmpullsecret
